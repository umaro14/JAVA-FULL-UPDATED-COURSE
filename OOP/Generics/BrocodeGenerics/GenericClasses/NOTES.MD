# generic class in Java
    ==> a class that can work with different data types while providing compile-time type safety.
It uses type parameters (like <T>) so that you can create objects of the same class but with different data types, without rewriting the class.